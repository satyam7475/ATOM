# ATOM desktop install.
#
# Keep one canonical dependency graph. This file intentionally delegates to
# requirements.txt so the desktop install cannot drift from the runtime tested
# by boot validation, voice/STT, MLX, RAG, MCP, and optional feature gates.

-r requirements.txt
